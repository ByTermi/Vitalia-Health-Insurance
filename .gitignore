node_modules/
.env
.env.local
.env.*.local
dist/
build/
.DS_Store
*.log
.next/
.turbo/
.cache/

# Local Claude Code settings (exclude sensitive config)
.claude/settings.local.json

# Personal vault snapshot (preferences, projects registry, secrets — not for team)
.claude/skills/vault/

# Personal invoice PDFs and financial data (kit-dashboard-facturas)
.claude/skills/kit-dashboard-facturas/facturas/
.claude/skills/kit-dashboard-facturas/facturas_datos.json

# Python virtual environment
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# Jupyter checkpoints
.ipynb_checkpoints/

# ML model weights (too large for git — use git-lfs or share separately)
*.h5
*.keras
# Note: .tflite files ARE committed (small, final artifacts for the app)

# Graphify knowledge graph output
graphify-out/

# Datasets — raw downloads and archives (too large, download separately)
data/compressed/
# Large raw datasets (download separately or use Git LFS per dataset)
data/raw/SisFall_dataset/
data/raw/pamap2+physical+activity+monitoring/
data/raw/pamap2+physical+activity+monitoring.zip
data/raw/human+activity+recognition+using+smartphones/UCI HAR Dataset.zip

# Preprocessed numpy arrays (derived from raw, regenerable via notebooks)
data/processed/*.npy
# Note: EDA figures (*.png) in data/processed/ ARE committed (small, for memoria)

# Kaggle upload staging folders (contain heavy .npy and binary dataset files)
scripts/vitalia-har-processed-upload/
scripts/pamap2-upload/
